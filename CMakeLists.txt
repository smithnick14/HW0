cmake_minimum_required (VERSION 2.6)

# Do NOT edit this file (unless you know CMake; but there's no reason to!!)

project (PersonalSpace)

set ( sources
	PersonalSpace.cpp )

add_executable(PersonalSpace ${sources})
